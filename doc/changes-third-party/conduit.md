# conduit (vendor)

The conduit library is vendored in `vendor/conduit/`.

## Disabled: mirage backend

`conduit-mirage` depends on the mirage stack which is not available in this
build.

- `src/conduit-mirage/dune`: replaced with a comment stub.
- `tests/conduit-mirage/simple/dune`: replaced with a comment stub.
